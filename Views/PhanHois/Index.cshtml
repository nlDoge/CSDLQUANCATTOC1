@model IEnumerable<QLQUANCATTOC.Models.PhanHoi>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DanhGia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BinhLuan)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaKhachHangNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DanhGia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BinhLuan)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaKhachHangNavigation.MaKhachHang)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaPhanHoi">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaPhanHoi">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaPhanHoi">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
